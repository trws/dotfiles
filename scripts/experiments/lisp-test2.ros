#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
; (progn ;;init forms
;   (ros:ensure-asdf)
;   #+quicklisp(ql:quickload '() :silent t)
;   )

(defpackage :ros.script.lisp-test2.3816022921
  (:use :cl))
(in-package :ros.script.lisp-test2.3816022921)

(defun main (&rest argv)
  (declare (ignorable argv)))
;;; vim: set ft=lisp lisp:
